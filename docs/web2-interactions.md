# Web2 交互复刻进度

## 范围

延续 Codex 浅色布局，依次实现顶部菜单、设置页面、Todo 与 Goal、斜杠命令、Diff 变化。公共接口与状态放在 `frontend/shared`；Web2 仅处理展示。

## 顶部菜单

文件／编辑／视图／帮助已接入真实动作：新对话、附件、Markdown 导出、草稿编辑与复制、搜索、面板显隐、代码更改、全屏、重连和帮助。支持方向键切换、Escape、点击外部关闭、禁用不可用操作。

验证：生产构建通过；14 项浏览器用例中 13 项首次通过，原有文件按钮定位因新增同名菜单发生歧义，限定面板后该用例复验通过。菜单方向键、草稿全选替换、设置入口已验证；真实服务页面无运行时异常。

## 后续模块

- 设置：已扩展为连接／对话／权限／关于四栏；连接保存前验证服务，错误保留在设置页。发送快捷键由共享控制器持久化，两版生效；当前模型和模式使用已有动作。构建、27 项单元测试、8 项相关浏览器用例通过（发送测试等待连接完成后复验）；真实 Chrome 页面已截图检查并修正弹窗居中。
- Todo：已接入引擎 TaskStore 的会话独立视图和 TaskCreateTool／TaskUpdateTool.execute()；共享 hook 管理加载、轮询、错误和状态。支持新增、依赖、状态调整、进度与输入区清单；Agent 运行期间拒绝并发手动写入。2 项真实 SQLite API 测试覆盖输入、依赖、终态、隔离、重开存储和并发；浏览器错误恢复测试通过，真实服务中新增并完成“Web2 实际验收：核对共享设置”，刷新后已恢复完成状态。Ruff 与生产构建通过。
- Goal：使用真实 GoalStore，与 Mission 区分，保留唯一未完成目标和追踪运行约束。
- 斜杠命令：复用命令索引与执行层，不能当作普通 LLM 消息。
- Diff：真实补丁解析、行号、文件筛选、暂存区分类及更新反馈。
