# Web2 具体执行摘要（2026-09-11）

## 简短流式思考摘要（2026-09-12）

- 活跃 Thinking 阶段新增一行“思考摘要”，在任务刚发送、进入工具执行、计划切换、
  执行恢复和工具结束时随真实事件更新。文案保持一行、透明背景和左侧细分隔线，
  使用轻量光标提示当前仍在运行，不增加新的卡片层级。
- 摘要仅根据用户目标、任务快照、工具公开动作及机械终态生成；不读取
  `thinking_delta`，不传输或展示模型内部推理。任务结束后的执行事实继续由原有阶段摘要
  和 Tool Chips 保留。

## 工具输出结构化渲染（2026-09-12）

- Tool Chips 展开后的工具输出按内容识别：完整 unified diff 复用审查面板差异视图，显示
  新旧行号、增删符号和浅色增删背景；带语言围栏的文件内容复用现有 `CodeBlock`，显示
  语言、复制和下载操作；无围栏的多行命令输出按终端代码块显示。
- 工具动作、执行状态、文件写入结果说明和截断提示仍作为普通文字展示。原始 diff、
  HTML 等代码围栏标记不会再作为正文显示；单次输出中的多个代码围栏分别渲染，围栏
  之间的说明文字仍按正文展示。普通多行说明不会误显示为代码，输入参数继续独立折叠。
- 差异解析继续使用共享 `parseUnifiedDiff()`，右侧审查面板和对话工具详情采用同一套
  行号及增删语义，避免两套解析规则产生不一致。

## 活跃任务与停滞反馈（2026-09-12）

- 运行中的 Thinking 标题不再固定显示“正在推理”。页面优先展示正在执行的具体工具；
  没有工具运行时展示任务计划里的 `in_progress` 项；上下文压缩或空响应恢复发生在计划
  之后时，展示“正在重新生成可执行步骤”。计划工具本身统一显示为“更新执行计划”。
- 活跃阶段使用现有 Beautiful UI Thinking、Tool Chips 和 `MessageContent`，继续保持透明
  布局。页面每秒根据真实事件时间更新累计运行时间与最后一次公开进展时间。
- 15 秒内显示刚刚有新进展；15 到 45 秒显示正在等待下一步；45 到 120 秒明确说明没有
  新的工具或公开输出；超过 120 秒提示任务可能停滞。终态事件到达后立即停止这些活跃提示。
- 状态推导只读取任务快照、工具公开动作、恢复事件、用户目标和事件时间戳；不收集或展示
  `thinking_delta` 内容，也不把 `busy=true` 解释为服务健康。

## 分阶段公开摘要与组件组合（2026-09-11）

- 后端新增 `phase_summary` Runtime 事件。引擎在一轮真实工具调用全部得到结果并写入
  tool message 后发布事件，事件携带实际 `turn`；每项仅包含经过公开动作投影的
  `action` 与机械 `status`，不读取、传输或持久化模型隐藏 reasoning。
- `progress_summary()` 根据上述事实生成中文阶段摘要，例如
  `本阶段已完成：读取文件：README.md。`；失败、停止、多于三项和重复动作均有
  有界规则。该摘要通过 SSE 的 `activity_summary` 发送，并由现有两条 run recorder
  路径保存为 `activity` step，因此刷新和历史回放后仍存在。
- Web2 按 ReAct `turn` 形成阶段。每阶段先显示现有 Beautiful UI Thinking 外壳及
  Tool Chips，再用现有 `MessageContent` 显示透明背景的公开摘要；下一轮工具出现在
  下一段 Thinking 中，最终回答仍位于所有阶段之后。
- 组件按真实内容选择：计划与上下文压缩使用 Steps，普通公开进度使用 Reasoning，
  浏览器与网页工具使用 Search，其余工具使用 Coding + Tool Chips。Search 没有
  合法 URL 时显示普通行，不生成空链接；官网查询和 `+7 more` 示例不会进入产品。
- 旧 run 没有 `phase_summary` 时，前端仅根据已经保存的 `public_action` 与工具终态
  生成兼容摘要。该回退不补造验收结论，也不会读取旧 `analysis.detail`。
- 当前阶段摘要是执行事实摘要，不是模型自由撰写的思维过程。它能说明计划在工作目录
  执行什么、读取或修改了什么、是否查看页面布局、是否压缩上下文及结果状态；产品是否
  修复仍由工具证据、测试和最终答复共同确认。
- 本轮验证通过：Python 事件、传输、持久化及引擎工具发布定向测试 115 项，前端映射
  单测 16 项，TypeScript、生产构建和 5 项浏览器场景通过；另 2 项需要显式真实会话
  环境变量的 Playwright 场景按设计跳过。生产构建仍有既有主包体积提示。
- 当前 `openai/kimi-for-coding` 真实会话形成 5 个 turn：读取入口文档、执行命令、
  搜索文件、读取被引用文档和最终整理。每轮工具之后出现独立公开摘要，最终回答位于
  全部阶段之后；页面刷新后仍恢复 5 个阶段与 4 条工具阶段摘要，摘要计算背景均为透明。

## Tool Chips 外观恢复（2026-09-11）

- 原因：公开执行摘要接入时使用了自定义长文本时间线，绕过原 Tool Chips 组件。
  现在重新使用用户提供的原版组件布局：28px 工具行、简短动作名、浅灰目标标签、
  可展开详情和文件变更标签。任务与工作目录、计划、上下文压缩仍来自真实公开事件。
- 文件行显示 basename，详情保留完整路径；长命令和摘要单行省略，展开可读完整记录。
  完整输入参数单独折叠，工具输出优先展示。每条记录使用稳定 id，重复工具不会一起展开。
- 新建文件行数取真实工具结果；完整 unified diff 才统计增删数量。同一路径的多次
  修改累计工具操作行数（不是工作区最终净 diff）；不同目录的同名文件仍分开。
  截断或缺失差异仅显示文件名及已有预览，失败操作不列为成功文件变更。
- 文件预览使用带主题样式的 portal，支持悬停、聚焦、点击和 Escape；历史每轮 run
  的归属保持不变。总览折叠后隐藏内容不可聚焦，390px 正文不横向溢出。
- 真实会话 `97e3d5b4a2e8` 的 run `7fddb68ed497` 已由当前 5174 页面回读：
  `creative_mode.html` 显示写入 284 行、`+284`，刷新后保持；
  截图 `.naumi/data/tool-chips-real.png`。本次没有额外调用模型或修改用户生成的 HTML。
- 验证：TypeScript、生产构建、14 项映射单测、工具输出/历史/公开摘要/真实会话的
  浏览器验证；Python `ruff check src/` 通过。此模块仅修改前端，不新增后端工具或命令。
  旧数据没有保存的 diff 无法恢复；前端构建仍有原有 bundle 大小提示。

## 问题

原执行过程仅有两句固定文案。TOOL_START 的公开传输丢弃所有参数，且页面
没有保留上下文压缩和任务快照事件，无法说明具体操作。历史回放还会优先展示
analysis.detail，不适合作为公开摘要来源。

## 实现

- 新增公共动作摘要构建器，只读取工具名称及有限的路径、命令、目标字段。
  不读取模型思考内容，不展示编辑正文、浏览器脚本或完整参数 JSON；敏感值脱敏，
  访问地址省略凭证、查询和片段，摘要有长度上限。
- 工具启动附带 activity_summary；持久化使用 metadata.public_action，结束事件
  保留原动作。引擎与 HTTP 两条记录路径复用相同生成规则。
- 任务快照与上下文压缩转换为公开进度行并持久化；压缩统计使用消息条数，
  不误标为 token 数。任务仅显示实际任务状态，未知操作保留名称。
- Web2 展示本次请求与工作目录、操作摘要与执行状态、上一步结果状态以及
  任务和压缩进展。历史记录从操作证据恢复，不读取 analysis.detail。
- 每个持久化 run 绑定到对应的用户消息，并在该轮助手回复之前渲染。切换到
  下一轮、向上滚动或刷新页面后，旧轮次的执行过程仍保留在原位置；旧记录中
  message id 未关联时，按请求摘要和时间顺序恢复对应关系。
- 不收集 thinking_delta，避免无可见信息的高频事件挤掉实际工具记录。

## 验证与边界

语义上的“问题已修复”“布局正确”须由具体工具证据和最终答复给出；
本摘要不会因工具返回 success 就宣称产品验收通过。历史数据没有保存压缩／任务
事件时无法补造；尚未产生具体操作时，只展示本次任务与工作目录。

- Python 事件传输、运行持久化与 API 回归共 103 项通过；48 项前端单元测试、
  TypeScript 与生产构建通过。新增断流／取消、重复投递、并行调用分离、
  旧记录回放、敏感字段过滤测试。旧后端的任务／压缩原始事件也支持回退生成摘要。
- 4 项浏览器场景通过（包含重跑）：具体文件操作、计划、压缩、刷新恢复、
  390px 换行与真实文件记录。真实场景会话 `6f2a4884b0f2` 使用当前服务同一
  数据目录，直接执行真实 file_read，读取本文件，经 RuntimeEvent、SSE 映射、
  SQLite 存储后由 5174／8765 页面读取；刷新后摘要仍显示具体文件路径。
  截图：`.naumi/data/public-activity-real.png`；证据：`.naumi/data/public-activity-live.json`。
- 实际模型调用遇到供应商 429（五小时用量上限），不能计作模型验收通过。
  后续真实场景是直接调用引擎工具执行管线，未使用 mock 工具或伪造模型回复。
- 全量 pytest 首个失败后未正常退出，已停止；额外架构检查 45 项通过、1 项跳过，
  `release_launcher_entry.py` 存在未归属模块的现有失败。未修改无关模块掩盖失败。
- 8765 后端已在用户授权范围内重启并加载当前代码，`/openapi.json` 返回 200；5173
  保持原监听进程不变。当前 Web2 已通过真实 Kimi 多轮工具和刷新恢复验证。
